#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR=$(cd -- "$(dirname -- "$0")" && pwd)
export PYTHONPATH="$SCRIPT_DIR/src${PYTHONPATH:+:$PYTHONPATH}"
CONFIG_PATH=${LABFLOW_CONFIG:-$SCRIPT_DIR/labflow.json}
exec python3 -m labflow --config "$CONFIG_PATH" "$@"
